find_package(PkgConfig REQUIRED)
pkg_check_modules(IPOPT REQUIRED ipopt)

list(APPEND @PROJECT_NAME@_INCLUDE_DIRS "${IPOPT_INCLUDE_DIRS}")
set(@PROJECT_NAME@_DEFINITIONS "-DBUILD_WITH_IPOPT_SUPPORT -DHAVE_CSTDDEF")